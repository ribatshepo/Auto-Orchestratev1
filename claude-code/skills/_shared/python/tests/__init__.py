"""
Test suite for shared Python libraries.

This package contains pytest tests for all layer modules in the shared Python library.
Tests are organized by layer (layer0, layer1, layer2, layer3) to match the source structure.
"""
