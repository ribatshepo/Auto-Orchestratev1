[pytest]
# Put the plugin root (claude-code/) on sys.path so the package-absolute test
# imports — `from lib.ci_engine...`, `from lib.artifact_envelope...` — resolve
# deterministically regardless of the working directory pytest is invoked from.
# Without this, collection only works by accident of pytest's rootdir insertion.
pythonpath = .
testpaths = lib
