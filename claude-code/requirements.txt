# Auto-Orchestrate CI Engine Dependencies
# Installed by install.sh during setup
# Version: 1.0.0
# Date: 2026-04-14

# =============================================================================
# Standard Library Modules Used (no pip install needed):
# =============================================================================
# json, os, sys, pathlib, datetime, hashlib, re, subprocess, shutil
# collections, dataclasses, typing, threading, time, sqlite3, fcntl
# tempfile, http.server, unittest.mock
#
# All CI engine modules (ci_engine/, domain_memory/) use Python standard
# library only. No third-party dependencies are required for core functionality.
# =============================================================================

# =============================================================================
# Test Dependencies (optional - only needed for running tests):
# =============================================================================
# pytest is used by test files in lib/ci_engine/tests/ and lib/domain_memory/tests/
# Install with: pip install pytest
#
# pytest>=7.0.0

# =============================================================================
# Future Dependencies (add here when third-party packages are introduced):
# =============================================================================
# If CI engine modules introduce third-party dependencies, list them below
# with pinned version ranges for reproducibility.
#
# Example format:
# pyyaml>=6.0,<7.0
# jsonschema>=4.17,<5.0
# requests>=2.28,<3.0

# No third-party dependencies required. All imports use Python standard library.
